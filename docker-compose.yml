version: '3'

services:
  nodejs:
    build:
      context: .
    image: nodejs
    container_name: nodejs
    restart: unless-stopped
    ports:
      - "80:8080"
    volumes:
      - .:/home/node/app
      - node_modules:/home/node/app/node_modules
volumes:
  node_modules: